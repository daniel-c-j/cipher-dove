import 'package:flutter/material.dart';

class SettingsScreenTabletLayout extends StatelessWidget {
  const SettingsScreenTabletLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Tablet.'),
    );
  }
}
