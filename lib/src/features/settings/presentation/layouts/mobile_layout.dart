import 'package:flutter/material.dart';

class SettingsScreenMobileLayout extends StatelessWidget {
  const SettingsScreenMobileLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mobile.'),
    );
  }
}
