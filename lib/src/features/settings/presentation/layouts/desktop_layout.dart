import 'package:flutter/material.dart';

class SettingsScreenDesktopLayout extends StatelessWidget {
  const SettingsScreenDesktopLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Desktop.'),
    );
  }
}
